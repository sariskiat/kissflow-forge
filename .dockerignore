# Secrets — NEVER ship into the image (Cloud Run injects env from Secret Manager at runtime).
.env
.env.*
*.pem
*.key

# Blindness / local-only tenant traces (screenshots, handoffs, capture scratch) — never ship.
HANDOFF.md
image*.png
kissflowfield-info-gatherlist/
.vscode/

# Not needed at runtime.
.git/
.github/
tests/
docs/adr/
docs/research/
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
requirements.txt
