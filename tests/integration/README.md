# Live integration tests

Run the live suites with `make test-live` after configuring the Kissflow dev tenant
credentials in the local environment. The suites are skipped unless `--run-live` is
provided and never run in CI.
